server:
  port: 8083

gcp:
  project-id: ${GCP_PROJECT_ID:sarasavi-enterprise-prod}
  bucket:
    name: ${GCP_BUCKET_NAME:sarasavi-media-bucket}

spring:
  servlet:
    multipart:
      max-file-size: 15MB
      max-request-size: 20MB

logging:
  level:
    lk.sarasavi: DEBUG
    com.google.cloud: INFO
